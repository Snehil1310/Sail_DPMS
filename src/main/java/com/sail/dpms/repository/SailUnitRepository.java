package com.sail.dpms.repository;

import com.sail.dpms.entity.SailUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SailUnitRepository extends JpaRepository<SailUnit, Long> {
}
